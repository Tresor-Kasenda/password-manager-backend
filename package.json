{
  "name": "password-manager",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "cd backend && make dev",
    "build": "npm run build -w frontend && cd backend && make build",
    "test": "npm run test -w frontend && cd backend && make test"
  }
}
